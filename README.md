# Statman

Call of Duty stats via discord bot


## Getting started

Edit `details.json` with your login details, and `usernames.py` with the users
you want to track (need to be COD friends).

```shell
git clone git@github.com:alxwrd/statman.git && cd statman
python -m pip install poetry
poetry install

poetry run python statman
```